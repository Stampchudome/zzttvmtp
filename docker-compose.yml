version: "3.9"

services:
  app:
    build: .
    ports:
      - "8000:8000"
    environment:
      - AI_MODEL=${AI_MODEL:-openai:gpt-4o}
      - AI_API_KEY=${AI_API_KEY}
      - HOST=0.0.0.0
      - PORT=8000
